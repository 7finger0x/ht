global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: hermes-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - api:8000
        labels:
          service: hermes-api
          environment: development
          release: local-compose
